# ポケモンアキネーター 設計書

## 作るもの

AIがポケモンを1体思い浮かべて、人間がチャットで質問しながら当てるゲーム。

---

## ゲームの流れ

1. microCMSに登録されたポケモンの中からランダムで1体選ぶ
2. 英語名でPokeAPIを叩いてタイプ・伝説かどうか・画像URLを取得
3. その情報をシステムプロンプトに渡す（AIだけが答えを知っている状態）
4. 人間がチャットでAIに質問する（「水タイプですか？」など）
5. AIはYes/Noのみで答える
6. 人間が答えを宣言したらAIが○か×を返す
7. 正解なら画像を表示してゲーム終了

---

## 使う技術

| 技術 | 用途 |
|---|---|
| Next.js | フロントエンド＋API Routes |
| microCMS | 出題するポケモンのリスト管理 |
| PokeAPI | ポケモンの詳細情報・画像の自動取得 |
| OpenAI API | Yes/No回答と正誤判定 |

---

## microCMSの設定

`pokemons` というエンドポイントを作成して以下のフィールドを登録する。

| フィールドID | 内容 |
|---|---|
| `name_ja` | ポケモンの日本語名（例：ゲンガー） |
| `name_en` | ポケモンの英語名・小文字（例：gengar） |

---

## PokeAPIで取得する情報

```
GET https://pokeapi.co/api/v2/pokemon/{name_en}
GET https://pokeapi.co/api/v2/pokemon-species/{name_en}
```

取得する項目：
- タイプ（types）
- 図鑑番号（id）
- 画像URL（sprites.front_default）
- 伝説かどうか（is_legendary）
- 幻かどうか（is_mythical）

---

## API Routesの役割

`/api/chat` というエンドポイントを作成する。

- OpenAI APIの呼び出しはすべてここで行う
- システムプロンプト（＝答えのポケモン情報）はサーバー側のみで保持する
- ブラウザからはこのAPI Routesにリクエストを送るだけにする
- これによりシステムプロンプトの中身がブラウザから見えなくなる

---

## システムプロンプトのイメージ

```
あなたはポケモンアキネーターのゲームマスターです。
以下のポケモンを思い浮かべています。ユーザーには絶対に名前を教えないでください。

【ポケモン情報】
- タイプ：{types}
- 図鑑番号：{id}
- 伝説のポケモン：{is_legendary}
- 幻のポケモン：{is_mythical}

【ルール】
- 質問にはYes/Noのみで答える
- ユーザーが答えを宣言したら○か×を返す
- 正解の場合は「正解です！」と答える
```

---

## 注意事項

- PokeAPIは英語名・小文字でリクエストする（例：gengar、pikachu）
- 会話履歴はフロントのstateで管理して毎回全履歴をAPI Routesに渡す
- OpenAI APIキーは`.env`に記載してサーバーサイドのみで使用する
